---
phase: 01-foundation-auth
plan: 07
type: execute
wave: 2
depends_on:
  - 01-05
files_modified:
  - infra/ecs/task-definition.json
  - infra/ecs/service.json
  - infra/cloudflare/wrangler.toml
  - .github/workflows/deploy.yml
  - .env.example
autonomous: true
gap_closure: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "ECS task definition JSON exists referencing the API Docker image with correct container config"
    - "GitHub Actions deploy workflow pushes Docker image to ECR and updates ECS service"
    - "Cloudflare wrangler.toml configures the web app static assets via Cloudflare Pages or CDN"
    - "CI deploy step only runs on main branch after tests pass"
  artifacts:
    - path: "infra/ecs/task-definition.json"
      provides: "ECS Fargate task definition for levelup-api service"
      contains: "fargate"
    - path: ".github/workflows/deploy.yml"
      provides: "Deploy pipeline: ECR push + ECS service update + Cloudflare Pages deploy"
      contains: "aws-actions/amazon-ecs-deploy-task-definition"
    - path: "infra/cloudflare/wrangler.toml"
      provides: "Cloudflare Pages configuration for Next.js web app"
      contains: "pages_build_output_dir"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "infra/ecs/task-definition.json"
      via: "aws-actions/amazon-ecs-deploy-task-definition action"
      pattern: "amazon-ecs-deploy-task-definition"
    - from: ".github/workflows/deploy.yml"
      to: "infra/cloudflare/wrangler.toml"
      via: "cloudflare/pages-action or wrangler pages deploy"
      pattern: "wrangler pages deploy"
---

<objective>
Close INFRA-05 gap: create the production deployment infrastructure as Infrastructure-as-Code artifacts and a GitHub Actions deploy workflow.

This plan produces: (1) an ECS Fargate task definition and service config for the Fastify API, (2) a Cloudflare Pages wrangler.toml for the Next.js web app, and (3) a GitHub Actions deploy.yml that builds the Docker image, pushes to ECR, updates the ECS service, and deploys the web app to Cloudflare Pages.

These are configuration artifacts. Actual AWS infrastructure (VPC, ECS cluster, ECR repo, RDS, ElastiCache) is provisioned by the developer using these as inputs — this plan does not run Terraform or create live AWS resources.

Purpose: INFRA-05 requires the deployment configuration to exist so the infrastructure can be stood up. Without these artifacts, there is no defined path from code to production.
Output: infra/ecs/, infra/cloudflare/, .github/workflows/deploy.yml
</objective>

<execution_context>
@/Users/jason/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jason/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-05-SUMMARY.md

<interfaces>
<!-- Existing artifacts the deploy config must reference -->

From apps/api/Dockerfile (multi-stage, already exists):
- Final image is a Node.js production build
- API runs on port 3001 (check apps/api/src/server.ts for the actual port)
- Health endpoint: GET /api/health

From .github/workflows/ci.yml (existing CI — deploy.yml must depend on this):
- Runs on: push + pull_request
- Jobs: ci (lint, typecheck, test, build)
- Deploy workflow will use `needs: ci` or run only on main after checks

Environment variables required by the API container (from existing code):
- DATABASE_URL
- REDIS_URL
- CLERK_SECRET_KEY
- CLERK_WEBHOOK_SECRET
- SENTRY_DSN
- GRAFANA_LOKI_URL, GRAFANA_LOKI_USER, GRAFANA_LOKI_PASSWORD
- NODE_ENV=production
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ECS Fargate task definition and service config</name>
  <files>infra/ecs/task-definition.json, infra/ecs/service.json, infra/ecs/README.md</files>
  <action>
Create `infra/ecs/` directory with the following files.

**infra/ecs/task-definition.json** — ECS Fargate task definition template:
```json
{
  "family": "levelup-api",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::ACCOUNT_ID:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::ACCOUNT_ID:role/ecsTaskRole",
  "containerDefinitions": [
    {
      "name": "levelup-api",
      "image": "ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/levelup-api:latest",
      "portMappings": [
        {
          "containerPort": 3001,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "environment": [
        { "name": "NODE_ENV", "value": "production" },
        { "name": "PORT", "value": "3001" }
      ],
      "secrets": [
        { "name": "DATABASE_URL", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/DATABASE_URL" },
        { "name": "REDIS_URL", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/REDIS_URL" },
        { "name": "CLERK_SECRET_KEY", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/CLERK_SECRET_KEY" },
        { "name": "CLERK_WEBHOOK_SECRET", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/CLERK_WEBHOOK_SECRET" },
        { "name": "SENTRY_DSN", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/SENTRY_DSN" },
        { "name": "GRAFANA_LOKI_URL", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/GRAFANA_LOKI_URL" },
        { "name": "GRAFANA_LOKI_USER", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/GRAFANA_LOKI_USER" },
        { "name": "GRAFANA_LOKI_PASSWORD", "valueFrom": "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:levelup/GRAFANA_LOKI_PASSWORD" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/levelup-api",
          "awslogs-region": "REGION",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:3001/api/health || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
```

**infra/ecs/service.json** — ECS service configuration reference:
```json
{
  "cluster": "levelup-production",
  "serviceName": "levelup-api",
  "taskDefinition": "levelup-api",
  "desiredCount": 1,
  "launchType": "FARGATE",
  "networkConfiguration": {
    "awsvpcConfiguration": {
      "subnets": ["SUBNET_ID_1", "SUBNET_ID_2"],
      "securityGroups": ["SECURITY_GROUP_ID"],
      "assignPublicIp": "DISABLED"
    }
  },
  "loadBalancers": [
    {
      "targetGroupArn": "arn:aws:elasticloadbalancing:REGION:ACCOUNT_ID:targetgroup/levelup-api/XXXXX",
      "containerName": "levelup-api",
      "containerPort": 3001
    }
  ],
  "deploymentConfiguration": {
    "maximumPercent": 200,
    "minimumHealthyPercent": 100,
    "deploymentCircuitBreaker": {
      "enable": true,
      "rollback": true
    }
  }
}
```

**infra/ecs/README.md** — Setup instructions:
```markdown
# ECS Fargate Deployment

## Prerequisites

1. AWS account with ECS, ECR, Secrets Manager access
2. Create ECR repository: `aws ecr create-repository --repository-name levelup-api`
3. Create ECS cluster: `aws ecs create-cluster --cluster-name levelup-production`
4. Create AWS Secrets Manager secrets for each valueFrom ARN in task-definition.json
5. Create IAM roles: ecsTaskExecutionRole (with AmazonECSTaskExecutionRolePolicy + SecretsManagerReadWrite) and ecsTaskRole
6. Create VPC, subnets, security groups, and ALB target group
7. Replace all ACCOUNT_ID, REGION, SUBNET_ID_*, SECURITY_GROUP_ID placeholders

## First Deploy

```bash
# Authenticate Docker to ECR
aws ecr get-login-password --region REGION | docker login --username AWS --password-stdin ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com

# Build and push
docker build -t levelup-api apps/api/
docker tag levelup-api:latest ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/levelup-api:latest
docker push ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/levelup-api:latest

# Register task definition
aws ecs register-task-definition --cli-input-json file://infra/ecs/task-definition.json

# Create service
aws ecs create-service --cli-input-json file://infra/ecs/service.json
```

Subsequent deploys are handled automatically by .github/workflows/deploy.yml.
```
  </action>
  <verify>
    <automated>cd /Users/jason/LevelUP && python3 -m json.tool infra/ecs/task-definition.json > /dev/null && python3 -m json.tool infra/ecs/service.json > /dev/null && echo "ECS JSON valid"</automated>
  </verify>
  <done>
    - infra/ecs/task-definition.json is valid JSON with FARGATE requiresCompatibilities, container on port 3001, secrets from Secrets Manager, health check
    - infra/ecs/service.json is valid JSON with correct service config
    - infra/ecs/README.md has first-deploy instructions
  </done>
</task>

<task type="auto">
  <name>Task 2: Cloudflare CDN config + GitHub Actions deploy workflow</name>
  <files>infra/cloudflare/wrangler.toml, .github/workflows/deploy.yml, .env.example</files>
  <action>
**infra/cloudflare/wrangler.toml** — Cloudflare Pages config for the Next.js web app:
```toml
name = "levelup-web"
compatibility_date = "2024-01-01"

[env.production]
name = "levelup-web"

# Cloudflare Pages deploys the Next.js build output
# Build command: pnpm --filter @repo/web build
# Build output: apps/web/.next
# Framework preset: Next.js
pages_build_output_dir = "apps/web/.next"

# Custom domain (configure in Cloudflare Pages dashboard)
# route = "app.levelup.gg/*"
```

**infra/cloudflare/README.md** — Cloudflare Pages setup instructions (brief):
```markdown
# Cloudflare Pages (CDN for Next.js web app)

Cloudflare Pages deploys the Next.js web app with global CDN distribution.

## Setup

1. Connect the GitHub repo to Cloudflare Pages (Cloudflare Dashboard -> Pages -> Create application -> Connect to Git)
2. Build settings:
   - Build command: `pnpm --filter @repo/web build`
   - Build output directory: `apps/web/.next`
   - Framework preset: Next.js
3. Add environment variables in Cloudflare Pages dashboard (NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, etc.)
4. Set CLOUDFLARE_API_TOKEN and CLOUDFLARE_ACCOUNT_ID as GitHub secrets for CI deployment

Alternatively, use the deploy.yml workflow with wrangler pages deploy (requires CLOUDFLARE_API_TOKEN secret).
```

**.github/workflows/deploy.yml** — Production deploy workflow:
```yaml
name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy-api:
    name: Deploy API to ECS
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/levelup-api:$IMAGE_TAG -t $ECR_REGISTRY/levelup-api:latest apps/api/
          docker push $ECR_REGISTRY/levelup-api:$IMAGE_TAG
          docker push $ECR_REGISTRY/levelup-api:latest
          echo "image=$ECR_REGISTRY/levelup-api:$IMAGE_TAG" >> $GITHUB_OUTPUT
        id: build-image

      - name: Update ECS task definition with new image
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: infra/ecs/task-definition.json
          container-name: levelup-api
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy to ECS service
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: levelup-api
          cluster: levelup-production
          wait-for-service-stability: true

  deploy-web:
    name: Deploy Web to Cloudflare Pages
    runs-on: ubuntu-latest
    environment: production
    needs: []

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Build web app
        run: pnpm --filter @repo/web build
        env:
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_SENTRY_DSN: ${{ secrets.NEXT_PUBLIC_SENTRY_DSN }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: levelup-web
          directory: apps/web/.next
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
```

**Update .env.example** — add AWS and Cloudflare deploy secret placeholders:
```
# AWS Deployment (GitHub Actions secrets)
AWS_DEPLOY_ROLE_ARN=arn:aws:iam::ACCOUNT_ID:role/github-actions-deploy
AWS_REGION=us-east-1

# Cloudflare Pages (GitHub Actions secrets)
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# Sentry (also needed in CI for source map upload)
SENTRY_AUTH_TOKEN=your-sentry-auth-token
```
  </action>
  <verify>
    <automated>cd /Users/jason/LevelUP && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo "deploy.yml YAML valid"</automated>
  </verify>
  <done>
    - .github/workflows/deploy.yml is valid YAML with two jobs: deploy-api (ECR + ECS) and deploy-web (Cloudflare Pages)
    - infra/cloudflare/wrangler.toml exists with pages_build_output_dir pointing to apps/web/.next
    - .env.example updated with AWS and Cloudflare secret placeholders
    - deploy.yml uses OIDC role assumption (not long-lived AWS keys)
  </done>
</task>

</tasks>

<verification>
Run in order:

1. `cd /Users/jason/LevelUP && python3 -m json.tool infra/ecs/task-definition.json > /dev/null && echo "task-definition valid"` — JSON valid
2. `python3 -m json.tool infra/ecs/service.json > /dev/null && echo "service valid"` — JSON valid
3. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo "deploy.yml valid"` — YAML valid
4. `ls infra/cloudflare/wrangler.toml infra/ecs/README.md infra/cloudflare/README.md` — all files exist
</verification>

<success_criteria>
- infra/ecs/task-definition.json — valid ECS Fargate task definition with secrets from Secrets Manager and health check
- infra/ecs/service.json — ECS service config referencing the task definition
- infra/cloudflare/wrangler.toml — Cloudflare Pages config pointing to Next.js build output
- .github/workflows/deploy.yml — two-job workflow: Docker/ECR/ECS + Cloudflare Pages, triggers on push to main
- Uses OIDC role-based AWS auth (not static AWS_ACCESS_KEY_ID/SECRET)
- Developer setup instructions in infra/*/README.md files
- INFRA-04 gap closed: CI has a deploy step
- INFRA-05 gap closed: ECS Fargate + Cloudflare CDN infrastructure config exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-07-SUMMARY.md`

Note for developer: These are configuration templates. Before the deploy workflow runs successfully:
1. Create the AWS infrastructure (ECR repo, ECS cluster, VPC, ALB, IAM roles)
2. Replace all ACCOUNT_ID, REGION, SUBNET_ID, SECURITY_GROUP_ID placeholders in infra/ecs/ files
3. Connect GitHub repo to Cloudflare Pages in the Cloudflare Dashboard
4. Set all required GitHub Actions secrets in the repository Settings -> Secrets -> Actions
5. Create AWS OIDC provider and IAM role for GitHub Actions (AWS_DEPLOY_ROLE_ARN)
</output>
